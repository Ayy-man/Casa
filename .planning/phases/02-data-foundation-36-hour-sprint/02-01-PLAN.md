---
phase: 02-data-foundation-36-hour-sprint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0001_initial_schema.sql
  - supabase/migrations/0002_seed.sql
  - src/types/database.types.ts
  - src/lib/env.ts
  - src/utils/supabase/client.ts
  - src/utils/supabase/server.ts
  - src/utils/supabase/middleware.ts
  - .env.local
  - package.json
autonomous: false
requirements: [DATA-01, DATA-03, DATA-04, DATA-07, DATA-08]
user_setup:
  - service: supabase
    why: "Operator applies the schema + seed SQL to the hosted project; no Supabase CLI dependency on the build path (D-05)"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard → Project aqsitrzbjokkkpcohple → Settings → API → Project API keys → anon/public (renamed from NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY)"
    dashboard_config:
      - task: "Run supabase/migrations/0001_initial_schema.sql then 0002_seed.sql against project aqsitrzbjokkkpcohple"
        location: "Supabase Studio → SQL Editor (or `supabase db push` at operator discretion)"

must_haves:
  truths:
    - "All 12 tables exist in Supabase project aqsitrzbjokkkpcohple with PK/FK constraints"
    - "agent_runs.idempotency_key has a UNIQUE constraint"
    - "26 properties, 4 agents (mode='shadow'), ~20 pricing_recs, ~30 agent_logs, ~6 exceptions are seeded"
    - "npm run gen:types regenerates src/types/database.types.ts from the live schema"
    - "A missing required Supabase env var throws a readable error naming the var, not a silent undefined"
    - "All three Supabase client factories read NEXT_PUBLIC_SUPABASE_ANON_KEY (no PUBLISHABLE_KEY references remain)"
  artifacts:
    - path: "supabase/migrations/0001_initial_schema.sql"
      provides: "12-table DDL with safety-mechanic columns"
      contains: "create table"
    - path: "supabase/migrations/0002_seed.sql"
      provides: "Narrative-mirror seed data"
      contains: "insert into"
    - path: "src/lib/env.ts"
      provides: "Zod-validated env object, throws on missing vars"
      exports: ["env"]
    - path: "src/types/database.types.ts"
      provides: "Generated Database type"
      contains: "Database"
    - path: "package.json"
      provides: "gen:types script + zod + nanoid deps"
      contains: "gen:types"
  key_links:
    - from: "src/utils/supabase/client.ts"
      to: "src/lib/env.ts"
      via: "validated env object replaces process.env reads + non-null assertions"
      pattern: "from \"@/lib/env\""
    - from: "src/utils/supabase/server.ts"
      to: "src/lib/env.ts"
      via: "validated env object"
      pattern: "from \"@/lib/env\""
---

<objective>
Deploy the 12-table Supabase schema and seed it with the narrative mock-data content, fix the env-var / cookie correctness gaps, and produce the generated `database.types.ts` that every downstream data module types against.

Purpose: This is the spine of the phase. Plans 02 and 03 cannot exist without the live schema and the generated types. It also closes the two day-1 correctness pitfalls (Pitfall 2 — env var name + cookie shape; Pitfall 11 — TypeScript type drift).

Output: `supabase/migrations/0001_initial_schema.sql`, `supabase/migrations/0002_seed.sql`, `src/lib/env.ts`, `src/types/database.types.ts`, env-renamed Supabase client factories, `package.json` with `gen:types` + `zod` + `nanoid`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-foundation-36-hour-sprint/02-CONTEXT.md
@.planning/phases/02-data-foundation-36-hour-sprint/02-PATTERNS.md
@.planning/research/PITFALLS.md

# Seed data sources — mirror the narrative content 1:1 (D-06)
@src/lib/mock-data/properties.ts
@src/lib/mock-data/pricing.ts
@src/lib/mock-data/agents.ts
@src/lib/mock-data/exceptions.ts

<interfaces>
<!-- Env-var read pattern the new env.ts replaces. Extracted from codebase. -->

From src/utils/supabase/client.ts (current — to be reworked):
```typescript
const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL;
const supabaseKey = process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;
export const createClient = () => createBrowserClient(supabaseUrl!, supabaseKey!);
```

From src/utils/supabase/server.ts (cookie handler is ALREADY correct getAll/setAll — do NOT rewrite it; only rename the env var):
```typescript
export const createClient = (cookieStore: Awaited<ReturnType<typeof cookies>>) => {
  return createServerClient(supabaseUrl!, supabaseKey!, {
    cookies: { getAll() {...}, setAll(cookiesToSet) {...} },
  });
};
```

From src/lib/utils.ts (the src/lib utility shape to copy — tiny, named export, no default):
```typescript
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";
export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)); }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 12-table schema and the narrative-mirror seed SQL</name>
  <read_first>
    - src/lib/mock-data/properties.ts (26 PROPERTIES rows — exact names, neighborhoods, rates, owners, owner emails/phones)
    - src/lib/mock-data/pricing.ts (PRICING_BASE 26-row array — property, current, recommended, change, reasoning)
    - src/lib/mock-data/agents.ts (AGENTS 4-row array — key, name, tagline, mode)
    - src/lib/mock-data/exceptions.ts (EXCEPTIONS array — the 7 anchor cards: id, type, typeLabel, urgency, category, agent, property, neighborhood, summary, suggested, source, createdAt)
    - .planning/phases/02-data-foundation-36-hour-sprint/02-CONTEXT.md (D-01..D-07 — schema scope, safety columns, seed fidelity)
    - .planning/research/PITFALLS.md (Pitfalls 3, 4, 6, 9 — the safety-mechanic columns)
  </read_first>
  <action>
    Create `supabase/migrations/0001_initial_schema.sql` — raw Postgres DDL, no Supabase CLI assumptions. Author `create table` statements for all 12 tables: `properties`, `bookings`, `guests`, `agents`, `agent_runs`, `agent_logs`, `pricing_recs`, `exceptions`, `action_log`, `turnovers`, `claims`, `knowledge_chunks`. Every table gets a primary key (`id` — uuid `default gen_random_uuid()` for new tables, or text id where the seed needs stable ids like `properties.id` = `p01..p26`). Add foreign keys where the data relates (`pricing_recs.property_id` → `properties.id`, `agent_logs.agent_id` → `agents.id`, `agent_runs.agent_id` → `agents.id`, `exceptions.property_id` → `properties.id` nullable for Portfolio-wide cards, `knowledge_chunks.property_id` → `properties.id`).

    Include the D-02 safety-mechanic columns NOW (retrofitting after rows exist is expensive):
    - `agent_runs.idempotency_key text UNIQUE` and `action_log.idempotency_key text`
    - Three-state lifecycle: `pricing_recs.status text not null default 'pending'` (allowed values pending|accepted|dispatched, enforce via CHECK), `exceptions.state text`, and an `executed_at timestamptz` column on `pricing_recs`, `exceptions`, and `action_log`
    - Shadow-mode trio (SAFE-01): `agents.mode text not null default 'shadow'` (CHECK shadow|live), `agent_runs.mode_at_run text`, `agent_logs.shadow_mode boolean not null default true`
    - Operator-race (SAFE-04): `exceptions.claimed_by text`, `exceptions.claimed_at timestamptz`
    - Stalled-agent watchdog (SAFE-05): `agent_runs.expected_callback_by timestamptz`, `agent_runs.status text not null default 'pending'`, `agent_runs.completed_at timestamptz`

    `knowledge_chunks` is a structural shell only (D-03) — `id`, `property_id` FK, `content text`, `created_at` — NO pgvector extension, NO embedding column, NO HNSW index (Phase 5). Do NOT add `turnovers.whatsapp_thread`, `turnovers.dispatch_state`, or `agent_logs.kb_chunks_used[]` (D-04 — later phases). Use Postgres-idiomatic column types: `text` over `varchar`, `timestamptz` over `timestamp`, `numeric(10,2)` for rates/costs, `integer` for counts. Document non-obvious type choices in SQL `--` comments. Use no extensions that break logical replication (realtime is Phase 3).

    Create `supabase/migrations/0002_seed.sql` — `insert into` statements mirroring the narrative mock-data 1:1 (D-06): 26 `properties` rows from `PROPERTIES` (ids `p01`..`p26`, exact names/neighborhoods/rates/owners), 4 `agents` rows from `AGENTS` ALL with `mode='shadow'` (even though `agents.ts` shows Ops/SOP as "Live" — D-02 / CONTEXT criterion 4 override that), ~20 `pricing_recs` rows from `PRICING_BASE` (status `pending` or `accepted`, FK to the matching property), ~6 `exceptions` rows from the `EXCEPTIONS` anchor cards (including the `pricing_week` mega-card row id 3), ~30 `agent_logs` rows composed to look like real Pricing-Agent output (mix of `shadow_mode=true`, varied properties, plausible reasoning text). Decide whether to include 1-2 sample `agent_runs` rows so the Decisions/Activity feed renders before the first n8n run, or leave `agent_runs` empty for n8n to populate — document the choice in a SQL comment (if left empty, Plan 03's feed must render a graceful empty state). Keep schema and seed in separate files so 0001 is replayable independent of seed.
  </action>
  <verify>
    <automated>test -f supabase/migrations/0001_initial_schema.sql && test -f supabase/migrations/0002_seed.sql && grep -c 'create table' supabase/migrations/0001_initial_schema.sql | grep -qx 12 && grep -q 'idempotency_key' supabase/migrations/0001_initial_schema.sql && grep -iq 'unique' supabase/migrations/0001_initial_schema.sql && echo "SCHEMA OK"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0001_initial_schema.sql` contains exactly 12 `create table` statements (one per: properties, bookings, guests, agents, agent_runs, agent_logs, pricing_recs, exceptions, action_log, turnovers, claims, knowledge_chunks)
    - `agent_runs.idempotency_key` carries a UNIQUE constraint (grep `unique` near `idempotency_key`)
    - `agents.mode` has a default of `'shadow'`; `pricing_recs.status` has a default of `'pending'`
    - No `vector`/`pgvector`/`extension` references (Phase 5 deferral); no `whatsapp_thread`/`dispatch_state` columns (Phase 4 deferral)
    - `supabase/migrations/0002_seed.sql` contains `insert into properties` with 26 rows and `insert into agents` with 4 rows, all 4 agent rows using `mode='shadow'`
    - SQL comments document at least the type-choice rationale and the agent_runs seed decision
  </acceptance_criteria>
  <done>Both migration files exist, 0001 has 12 tables + the UNIQUE idempotency constraint + safety columns, 0002 mirrors the narrative mock-data, schema and seed are in separate replayable files.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <what-built>The two versioned migration files `supabase/migrations/0001_initial_schema.sql` and `supabase/migrations/0002_seed.sql` are written and committed to the repo. Per D-05, applying them to the hosted Supabase project is operator-run — there is no Supabase CLI dependency on the build path.</what-built>
  <how-to-verify>
    1. Open Supabase Studio for project `aqsitrzbjokkkpcohple` → SQL Editor (or run `supabase db push` if you prefer the CLI — your discretion).
    2. Run the full contents of `supabase/migrations/0001_initial_schema.sql`. Confirm it completes with no errors and 12 tables appear under Database → Tables.
    3. Run the full contents of `supabase/migrations/0002_seed.sql`. Confirm `properties` shows 26 rows and `agents` shows 4 rows (all `mode='shadow'`).
    4. Confirm the `anon`/`public` API key for the project — you will paste it into `.env.local` in the next task. (Note: per PROJECT.md, no Row-Level Security this milestone — the anon key reading these tables is an accepted, project-decided risk.)
  </how-to-verify>
  <resume-signal>Type "applied" once both SQL files have run successfully against project aqsitrzbjokkkpcohple and the table/row counts check out, or describe any SQL errors.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Rename env var, build the Zod env validator, wire gen:types and generate database.types.ts</name>
  <read_first>
    - src/utils/supabase/client.ts (current process.env read + non-null assertion)
    - src/utils/supabase/server.ts (current read; cookie handler is already correct — leave it)
    - src/utils/supabase/middleware.ts (current read)
    - src/lib/utils.ts (the src/lib named-export utility shape to mirror)
    - package.json (scripts block lines 5-10, dependencies block)
    - .env.local (current NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY entry)
    - .planning/phases/02-data-foundation-36-hour-sprint/02-CONTEXT.md (DATA-07, DATA-08)
  </read_first>
  <action>
    Add `zod@^3.23.8` and `nanoid@^5.0.7` to `package.json` `dependencies` (everything else stays pinned — CLAUDE.md). Add a `gen:types` entry to `scripts`: `supabase gen types typescript --project-id aqsitrzbjokkkpcohple > src/types/database.types.ts` (manual invocation is sufficient — no pre-commit hook). Run `npm install` to lock the new deps.

    Create `src/lib/env.ts` — a named-export Zod validator (no default export, mirroring `src/lib/utils.ts`). Define a Zod schema requiring `NEXT_PUBLIC_SUPABASE_URL` (`.url()`) and `NEXT_PUBLIC_SUPABASE_ANON_KEY` (`.min(1)`). Parse `process.env` at module load with `schema.parse(...)`; on failure, throw an Error whose message names the missing/invalid var (use Zod's `.error.issues` to build a readable message — e.g. `Missing required env var: NEXT_PUBLIC_SUPABASE_ANON_KEY`). Export the parsed object as `env` and infer its type with `z.infer<typeof schema>`. This file must run server-side at module load so misconfiguration fails loud at startup, not silently `undefined` at request time.

    Rename the env var across all three Supabase client factories: in `src/utils/supabase/client.ts`, `server.ts`, and `middleware.ts`, replace `process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` with the validated `env` object — import `{ env }` from `@/lib/env` and use `env.NEXT_PUBLIC_SUPABASE_URL` / `env.NEXT_PUBLIC_SUPABASE_ANON_KEY`, dropping the `!` non-null assertions. Do NOT touch the `getAll`/`setAll` cookie handler in `server.ts` / `middleware.ts` — it is already correct (DATA-08 needs runtime proof, not a rewrite).

    Rename the key in `.env.local`: `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` → `NEXT_PUBLIC_SUPABASE_ANON_KEY` (leave `NEXT_PUBLIC_SUPABASE_URL` unchanged; paste the anon key confirmed in the operator checkpoint).

    Run `npm run gen:types` to generate `src/types/database.types.ts` from the live schema, and commit the generated file (Pitfall 11 — types are committed, regenerated after every migration).
  </action>
  <verify>
    <automated>grep -rL 'NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY' src/utils/supabase/*.ts >/dev/null 2>&1; ! grep -rq 'NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY' src/ && grep -q 'gen:types' package.json && grep -q '"zod"' package.json && grep -q '"nanoid"' package.json && test -f src/types/database.types.ts && grep -q 'Database' src/types/database.types.ts && grep -q 'NEXT_PUBLIC_SUPABASE_ANON_KEY' src/lib/env.ts && npx tsc --noEmit && echo "ENV+TYPES OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY src/` returns nothing — the old key name is fully gone from source
    - `src/lib/env.ts` exports `env`, imports `zod`, parses at module load, and references `NEXT_PUBLIC_SUPABASE_ANON_KEY`
    - All three of `src/utils/supabase/{client,server,middleware}.ts` import `{ env }` from `@/lib/env` and have no `!` non-null assertion on the Supabase URL/key
    - The `getAll`/`setAll` cookie handler in `server.ts` and `middleware.ts` is byte-for-byte unchanged from before
    - `package.json` `scripts` has a `gen:types` entry; `dependencies` has `zod` (`^3.23.8`) and `nanoid` (`^5.0.7`)
    - `src/types/database.types.ts` exists, exports/contains `Database`, and reflects all 12 tables
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>Env var renamed everywhere, `src/lib/env.ts` Zod validator throws readable errors on missing vars, `gen:types` script wired, `database.types.ts` generated and committed, `zod`+`nanoid` installed, type-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Supabase | Operator runs DDL/seed SQL against the hosted project (D-05) |
| browser → Supabase | Client-side reads via the public `NEXT_PUBLIC_SUPABASE_ANON_KEY` |
| build env → app | `.env.local` / Vercel env vars consumed by the Supabase client factories |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Anon-key-readable tables, no RLS | accept | PROJECT.md explicitly defers RLS + real auth to v2 — single-tenant + shared demo creds make RLS premature. Documented accepted risk, not a blocker. |
| T-02-02 | Information Disclosure | Env-var / secret handling in `src/lib/env.ts` | mitigate | Validator only ever reads `NEXT_PUBLIC_*` vars (already public by Next.js convention). No `NEXT_PUBLIC_*_SECRET` introduced; no service-role key in client-reachable code. `.env.local` stays gitignored. |
| T-02-03 | Tampering | `agent_runs.idempotency_key` integrity | mitigate | `UNIQUE` constraint authored in 0001 from day one so duplicate n8n writes are rejected at the DB layer (Pitfall 3) — not retrofitted after rows exist. |
| T-02-04 | Spoofing/Tampering | Migration SQL applied to wrong project | mitigate | Operator checkpoint names the exact project id `aqsitrzbjokkkpcohple`; `gen:types` script pins the same `--project-id`. |
| T-02-SC | Tampering | npm installs (`zod`, `nanoid`) | mitigate | Both packages verified in RESEARCH.md package legitimacy audit (if table absent, treat as `[ASSUMED]` and verify on npmjs.com before install — see note below). |
</threat_model>

<verification>
- `npm run build` succeeds with no broken imports.
- `npx tsc --noEmit` passes.
- Supabase Studio for `aqsitrzbjokkkpcohple` shows all 12 tables with 26 properties + 4 agents seeded.
- A deliberately blanked `NEXT_PUBLIC_SUPABASE_ANON_KEY` in `.env.local` makes `src/lib/env.ts` throw a message naming the var (manual spot-check, then restore).
</verification>

<success_criteria>
- ROADMAP Phase 2 criterion 1 met: 12-table schema deployed, `agent_runs.idempotency_key` UNIQUE.
- ROADMAP Phase 2 criterion 2 met: `gen:types` produces `database.types.ts`; `src/lib/env.ts` throws readable errors; env var renamed across the three client factories; cookie handler validated as `getAll`/`setAll`.
- ROADMAP Phase 2 criterion 4 (seed) met: narrative-mirror seed of 26 properties + 4 shadow agents + ~20 pricing_recs + ~30 agent_logs + ~6 exceptions.
</success_criteria>

<output>
Create `.planning/phases/02-data-foundation-36-hour-sprint/02-01-SUMMARY.md` when done.
</output>
