---
phase: 01-casa-360-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/mock-data/exceptions.ts
  - src/lib/mock-data/properties.ts
  - src/lib/mock-data/cleanings.ts
  - src/lib/mock-data/owners.ts
  - src/lib/mock-data/pipeline.ts
  - src/lib/mock-data/financials.ts
  - src/lib/mock-data/compliance.ts
  - src/lib/mock-data/assistant.ts
  - src/lib/mock-data/index.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "All 26 mock properties use real Vancouver addresses with correct neighborhoods"
    - "Every cleaner reference uses Casa's real roster (Andrea, Carly, Sabrina, Juli, Stana, Andrea L.) — no Maria L. / Jason K. / Priya S. placeholders survive"
    - "EXCEPTIONS contains the 7 verbatim anchor exception cards plus 5-10 more across mixed categories"
    - "Every exception entry carries a category, a Suggested: string, a source attribution, an ISO createdAt, and a neighborhood"
    - "Vault surfaces have backing data: owners, pipeline, financials, compliance, and assistant canned responses all export typed mock modules"
  artifacts:
    - path: "src/lib/mock-data/exceptions.ts"
      provides: "Narrative EXCEPTIONS array with category/suggested/source/createdAt/neighborhood; ExceptionItem type; URGENCY_RANK"
      contains: "createdAt"
    - path: "src/lib/mock-data/owners.ts"
      provides: "OWNERS array + Owner type + getOwner helper"
      exports: ["OWNERS", "getOwner"]
    - path: "src/lib/mock-data/assistant.ts"
      provides: "Role-aware canned Assistant responses and suggested prompts"
      exports: ["ASSISTANT_RESPONSES"]
    - path: "src/lib/mock-data/index.ts"
      provides: "Barrel re-exporting all mock-data modules including the 5 new ones"
      contains: "owners"
  key_links:
    - from: "src/lib/mock-data/index.ts"
      to: "src/lib/mock-data/owners.ts"
      via: "export * barrel re-export"
      pattern: "export \\* from \"./owners\""
---

<objective>
Rewrite the mock-data layer so the redesign renders narrative, specific, operator-grade content: 26 real Vancouver addresses, Casa's real cleaner roster, the 7 anchor exception cards (verbatim from the brief) plus 5-10 more, and five new modules (owners, pipeline, financials, compliance, assistant) so the Vault and Assistant slices have backing data.

Purpose: Slices 03 (Exception Board), 04 (Vault), and 06 (Assistant) all read from these modules. This plan supplies the data contract those slices render — the `exceptions.ts` field additions (`category`, `suggested`, `source`, `createdAt`, `neighborhood`) and the 5 new modules are prerequisites. Module *structure* is preserved (Phase 2 wires the `src/lib/data/*` seam); only *content* and *shape additions* change.
Output: Rewritten `exceptions.ts`, `properties.ts`, `cleanings.ts`; new `owners.ts`, `pipeline.ts`, `financials.ts`, `compliance.ts`, `assistant.ts`; extended `index.ts` barrel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-casa-360-redesign/REDESIGN-BRIEF.md
@.planning/phases/01-casa-360-redesign/01-UI-SPEC.md
@.planning/phases/01-casa-360-redesign/01-PATTERNS.md

<interfaces>
<!-- Current exceptions.ts shape — from src/lib/mock-data/exceptions.ts -->
ExceptionItem = { id, type, typeLabel, urgency, agent, property, summary, timeAgo, actions, requiresConfirm? }
URGENCY_RANK: Record<Urgency, number>  — keep verbatim

<!-- Target ExceptionItem additions per 01-PATTERNS.md "exceptions.ts rewrite" -->
ADD: category: "Guest"|"Cleaner"|"Pricing"|"Maintenance"|"Owner"|"Revenue"|"Pipeline"|"Compliance"
ADD: neighborhood: string
ADD: suggested: string
ADD: source: string   (e.g. "Guest Agent · BK-2847")
ADD: createdAt: string  (ISO — replaces the static timeAgo string; date-fns computes time-ago at render)

<!-- Module template — from src/lib/mock-data/properties.ts:1-53 -->
export type X = { id: string; ... };
export const XS: X[] = [ ... ];
export const getX = (id: string): X | undefined => XS.find((x) => x.id === id);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite exceptions.ts with narrative cards and the new field contract</name>
  <read_first>
    - src/lib/mock-data/exceptions.ts (the file being modified — current ExceptionItem type, EXCEPTIONS array, URGENCY_RANK, AGENT_ACTIVITY)
    - .planning/phases/01-casa-360-redesign/REDESIGN-BRIEF.md ("Seed exception cards" — the 7 anchor cards with verbatim copy; "Category color coding" + "Action buttons by category" tables)
    - .planning/phases/01-casa-360-redesign/01-UI-SPEC.md (Copywriting Contract — per-category primary/secondary/tertiary CTA copy; "8 Exception-Category color quartets")
    - .planning/phases/01-casa-360-redesign/01-PATTERNS.md (section "exceptions.ts rewrite" — exact field additions)
  </read_first>
  <action>
    Rewrite src/lib/mock-data/exceptions.ts keeping the `Urgency` type, `URGENCY_RANK`, and the exported `ExceptionItem` type name + `EXCEPTIONS` array name (module structure preserved per L1).

    Extend `ExceptionItem` with: `category` (the 8-value union — `"Guest" | "Cleaner" | "Pricing" | "Maintenance" | "Owner" | "Revenue" | "Pipeline" | "Compliance"`), `neighborhood: string`, `suggested: string`, `source: string`, `createdAt: string` (ISO timestamp — replaces the static `timeAgo` string; the Exception Board computes time-ago with `date-fns/formatDistanceToNow`). Keep the existing optional `requiresConfirm` flag and the `actions` array.

    Seed the 7 anchor cards VERBATIM from the brief's "Seed exception cards" list — copy title, property, neighborhood, narrative body, `Suggested:` text, action verbs, and source attribution exactly:
    1. Critical · Guest — hot-water issue, 989 Nelson St (Downtown), source `Guest Agent · BK-2847`, ~9 min ago
    2. Critical · Cleaner — Andrea no-response, 1455 Howe St (Yaletown), source `Ops Agent · BK-2851`, ~1 hr ago
    3. Medium · Pricing — Taylor Swift weekend, Portfolio-wide (12 properties), source `Pricing Agent`, ~2 hr ago
    4. Medium · Pipeline — Sarah Chen callback, 2105 W 4th Ave (Kitsilano), source `Sales Agent`, ~5 hr ago
    5. Medium · Compliance — STR license expiry, 1120 Hamilton St (Yaletown), source `System`, ~12 hr ago
    6. Low · Owner — November revenue, 3280 W Broadway (Point Grey), source `Owner Agent`, ~8 hr ago
    7. Low · Revenue — Coal Harbour underperformance, 1233 W Cordova St (Coal Harbour), source `Owner Agent`, ~1 day ago

    Compute each `createdAt` as a real ISO string offset back from "now" by the stated interval (use 2026-05-20 as the reference date per the current-date context). Then add 5-10 more narrative exception cards across mixed urgency and the remaining categories (Maintenance, more Guest/Cleaner/Pricing) — Claude's Discretion on content, but each must follow the card contract: real Vancouver address + neighborhood from the brief's address list, a 2-3 sentence narrative body, a `Suggested:` string, a source attribution, an ISO `createdAt`. Set per-category `actions` arrays using the UI-SPEC Copywriting Contract verbs.

    One of the Pricing cards must be the "Pricing Week of [date]" portfolio mega-card — give it a distinct `type` value (e.g. `pricing_week`) so the Exception Board can render it with the bulk-approve side-sheet trigger.

    Keep the `AGENT_ACTIVITY` export; refresh its cleaner/property names to the real roster + real addresses.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && node -e "const {EXCEPTIONS}=require('./src/lib/mock-data/exceptions.ts'); " 2>/dev/null || grep -c 'createdAt:' src/lib/mock-data/exceptions.ts | awk '{ if ($1 >= 12) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `ExceptionItem` includes `category`, `neighborhood`, `suggested`, `source`, `createdAt`
    - `EXCEPTIONS` has at least 12 entries (7 anchors + >=5 more); every entry has a non-empty `suggested` and `source`
    - the 7 anchor cards match the brief's copy (hot-water/989 Nelson, Andrea/1455 Howe, Taylor Swift/portfolio, Sarah Chen/2105 W 4th, STR license/1120 Hamilton, November revenue/3280 W Broadway, Coal Harbour/1233 W Cordova)
    - one Pricing entry is a portfolio "Pricing Week of" mega-card with a distinct `type`
    - `URGENCY_RANK` and the `Urgency` type are unchanged
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>EXCEPTIONS reads as a real operator's exception queue; every card carries the category/suggested/source/createdAt/neighborhood contract the Exception Board renders.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite properties + cleanings to real Vancouver data</name>
  <read_first>
    - src/lib/mock-data/properties.ts (the file being modified — current Property type, PROPERTIES array, getProperty helper)
    - src/lib/mock-data/cleanings.ts (the file being modified — current cleaner-name references)
    - .planning/phases/01-casa-360-redesign/REDESIGN-BRIEF.md ("Real Vancouver addresses" — the 36-address list; "Cleaner roster (real)")
    - .planning/phases/01-casa-360-redesign/01-PATTERNS.md (section "properties.ts" / "cleanings.ts" MODIFY rows)
  </read_first>
  <action>
    Rewrite src/lib/mock-data/properties.ts: keep the `Property` type, the `PROPERTIES` const name, and the `getProperty` helper (and any other existing exported helpers/types). Replace all 26 property records with real Vancouver addresses drawn from the brief's address list, each with its correct neighborhood (1455 Howe St / Yaletown, 989 Nelson St / Downtown, 1233 W Cordova St / Coal Harbour, etc.). Keep every existing field on the `Property` type populated with plausible narrative values. The `picsum.photos` image URLs already configured in `next.config.mjs` may stay as placeholder photos.

    Rewrite src/lib/mock-data/cleanings.ts: replace every cleaner-name reference with Casa's real roster — Andrea, Carly, Sabrina, Juli (main team); Stana, Andrea L. (Langley team). Remove any `Maria L.` / `Jason K.` / `Priya S.` placeholder. Keep the module's existing type and export shape. Update property references in cleanings to the real addresses so the data is internally consistent.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && ! grep -rnE 'Maria L\.|Jason K\.|Priya S\.' src/lib/mock-data/ && grep -q '1455 Howe' src/lib/mock-data/properties.ts && grep -q 'Coal Harbour' src/lib/mock-data/properties.ts</automated>
  </verify>
  <acceptance_criteria>
    - `PROPERTIES` has 26 entries, all with real Vancouver addresses + correct neighborhoods
    - `grep` for `Maria L.` / `Jason K.` / `Priya S.` across `src/lib/mock-data/` returns 0 matches
    - cleaner references across `cleanings.ts` use only Andrea / Carly / Sabrina / Juli / Stana / Andrea L.
    - the `Property` type and `getProperty` helper are unchanged in signature
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The property roster and cleaning board read as Casa's real 26-home Vancouver portfolio with the real cleaner team.</done>
</task>

<task type="auto">
  <name>Task 3: Create the 5 new Vault + Assistant mock-data modules and extend the barrel</name>
  <read_first>
    - src/lib/mock-data/properties.ts (analog for owners.ts / pipeline.ts / compliance.ts — type + SCREAMING_SNAKE const + getX helper template)
    - src/lib/mock-data/reports.ts (analog for financials.ts)
    - src/lib/mock-data/agents.ts (analog for assistant.ts — the PROMPT_VERSIONS flat-array shape)
    - src/lib/mock-data/index.ts (the barrel being modified — current export * lines)
    - .planning/phases/01-casa-360-redesign/REDESIGN-BRIEF.md (Vault card table — Owners "18 owners", Pipeline "34 prospects", Financials "P&L Summary", Compliance "certificates"; "Canned responses" examples; Assistant suggested prompts)
    - .planning/phases/01-casa-360-redesign/01-PATTERNS.md (section "Mock-data modules (CREATE + MODIFY)")
  </read_first>
  <action>
    Create five new modules in src/lib/mock-data/ following the existing module template (an exported `type`, a SCREAMING_SNAKE const array, a `getX(id)` helper where an id lookup makes sense):
    - `owners.ts` — `Owner` type + `OWNERS` array (18 owners — names consistent with the property/exception data, e.g. James Park, Sarah Chen) + `getOwner`. Each owner links to one or more property ids.
    - `pipeline.ts` — `Prospect` type + `PIPELINE` array (34 prospects — lead score, address, status) + `getProspect`. Modeled on `bookings.ts`.
    - `financials.ts` — a P&L summary structure modeled on `reports.ts` (per-property and portfolio revenue/expense/net figures, monthly breakdown).
    - `compliance.ts` — `Certificate` type + `COMPLIANCE` array (STR licenses, insurance, fire-safety certs with expiry dates — one entry consistent with the 1120 Hamilton STR-license exception card) + `getCertificate`.
    - `assistant.ts` — role-aware canned Assistant content modeled on the `agents.ts` `PROMPT_VERSIONS` flat-array shape. Export the 4 owner suggested prompts + 4 operations suggested prompts and a canned response per prompt. Use the brief's three worked examples verbatim ("Which properties are underperforming?", "What's our occupancy this month?", "Show me today's turnovers") and write matching narrative canned answers for the remaining role-specific prompts. Export a lookup keyed by prompt text and a `ASSISTANT_RESPONSES` (or similarly named) export.

    Extend src/lib/mock-data/index.ts: add `export * from "./owners"`, `export * from "./pipeline"`, `export * from "./financials"`, `export * from "./compliance"`, `export * from "./assistant"` — matching the existing barrel pattern. Ensure no export-name collisions across modules (rename if a generic name like `getX` collides).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && for f in owners pipeline financials compliance assistant; do test -f src/lib/mock-data/$f.ts || exit 1; grep -q "./$f" src/lib/mock-data/index.ts || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/mock-data/{owners,pipeline,financials,compliance,assistant}.ts` all exist and export typed data
    - `owners.ts` exports `OWNERS` (18 entries) + `getOwner`; `pipeline.ts` exports a 34-entry prospects array
    - `assistant.ts` exports role-aware suggested prompts + canned responses including the brief's 3 verbatim examples
    - `src/lib/mock-data/index.ts` re-exports all 5 new modules via `export *`
    - no export-name collision (build succeeds)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The Vault's 8 cards and the Assistant's role-aware demo all have typed narrative mock-data backing; the barrel exposes everything.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mock-data string -> rendered card | Narrative mock-data strings (exception bodies, suggested text, owner names) are rendered into React components downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering / XSS | narrative mock-data strings rendered into exception/Vault cards | accept | All mock-data is author-controlled static TypeScript literals — no user input, no network source. React escapes string children by default. Mitigation: the executor MUST NOT introduce `dangerouslySetInnerHTML` when rendering any mock-data string in the consuming slices (03/04/06). No HIGH-severity surface exists for static literals. |
| T-01-05 | Information Disclosure | real Vancouver addresses + owner names in mock data | accept | Addresses are public real-estate data; owner names are fictional/illustrative for a single-tenant demo. No real PII. Consistent with the demo-data posture in CLAUDE.md. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 — every new module type-checks and the barrel has no collisions.
- `EXCEPTIONS` contains the 7 anchor cards verbatim plus >=5 more, every card with a `Suggested:` string and source.
- `grep -rn 'Maria L.\|Jason K.\|Priya S.' src/lib/mock-data/` returns nothing.
- All 26 properties use real Vancouver addresses with correct neighborhoods.
</verification>

<success_criteria>
- ROADMAP SC6 (data half): mock data is rewritten narrative — 26 real Vancouver addresses with correct neighborhoods, Casa's real cleaner roster, 7 anchor exception cards + 5-10 more, every card with a `Suggested:` block + source.
- ROADMAP SC2 (data prerequisite): every exception carries `category`, `suggested`, `source`, `createdAt`, `neighborhood`.
- ROADMAP SC3 (data prerequisite): owners / pipeline / financials / compliance modules back the Vault's 8 cards.
- ROADMAP SC6 (Assistant prerequisite): `assistant.ts` supplies role-aware prompts + canned responses.
</success_criteria>

<output>
Create `.planning/phases/01-casa-360-redesign/01-02-SUMMARY.md` when done.
</output>
