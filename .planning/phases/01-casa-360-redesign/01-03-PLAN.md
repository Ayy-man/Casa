---
phase: 01-casa-360-redesign
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - src/components/casa/exception-card.tsx
  - src/app/(dashboard)/page.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "The home route / renders the Exception Board: a role-aware greeting block, 4 status pills, 7 multi-select filter chips, and an urgency-sorted stack of narrative exception cards"
    - "Each exception card shows a category pill, an inner 2px colored category rule, a time-ago, a Playfair title, a property/neighborhood subtitle, a 15px narrative body, a Suggested: italic block, category-specific action buttons, and a source-attribution footer"
    - "The 4 KPI tiles, the status banner, and the right rail are removed from the home page"
    - "Selecting a filter chip narrows the card stack; an empty result shows the 'All clear.' empty state"
    - "A 'Pricing Week of [date]' mega-card opens a side-sheet containing the 26-row Approve/Edit/Reject bulk table"
  artifacts:
    - path: "src/components/casa/exception-card.tsx"
      provides: "Narrative exception card with category pill, inner rule, Suggested block, source footer, action row"
      exports: ["ExceptionCard"]
      min_lines: 80
    - path: "src/app/(dashboard)/page.tsx"
      provides: "Exception Board home route — greeting, status pills, filter chips, card stack, empty state, Pricing mega-card sheet"
      contains: "useRole"
  key_links:
    - from: "src/app/(dashboard)/page.tsx"
      to: "src/components/casa/exception-card.tsx"
      via: "renders ExceptionCard per exception in the sorted/filtered stack"
      pattern: "ExceptionCard"
    - from: "src/app/(dashboard)/page.tsx"
      to: "src/lib/mock-data/exceptions.ts"
      via: "imports EXCEPTIONS + URGENCY_RANK"
      pattern: "EXCEPTIONS"
    - from: "src/components/casa/exception-card.tsx"
      to: "date-fns"
      via: "formatDistanceToNow on exception.createdAt"
      pattern: "formatDistanceToNow"
---

<objective>
Build the Exception Board — the new home route `/`. This is the operator's first surface every morning: a role-aware greeting, 4 data-driven status pills, 7 multi-select filter chips, and an urgency-sorted stack of narrative exception cards, each card a self-contained `casa/exception-card.tsx`. The standalone Pricing Approval Queue folds in as a "Pricing Week of [date]" mega-card that opens the preserved 26-row bulk-approve side-sheet.

Purpose: This is ROADMAP SC2 — the heart of the redesign and the surface the "10 minutes to inbox zero" core value runs through. After this plan a real operator can open `/`, see their day's exceptions, filter them, and reach the Pricing bulk-approve workflow.
Output: New `casa/exception-card.tsx`; rewritten `(dashboard)/page.tsx` (the Exception Board).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-casa-360-redesign/REDESIGN-BRIEF.md
@.planning/phases/01-casa-360-redesign/01-UI-SPEC.md
@.planning/phases/01-casa-360-redesign/01-PATTERNS.md
@.planning/phases/01-casa-360-redesign/01-01-SUMMARY.md
@.planning/phases/01-casa-360-redesign/01-02-SUMMARY.md

<interfaces>
<!-- From plan 01-02: ExceptionItem now carries these fields -->
ExceptionItem = { id, type, typeLabel, urgency, agent, property, neighborhood, summary,
                  category, suggested, source, createdAt (ISO), actions, requiresConfirm? }
URGENCY_RANK: Record<Urgency, number>  ("Critical":0 ... "Low":3)

<!-- From plan 01-01: available in globals.css + auth -->
useRole(): "owner" | "operations"   from @/lib/auth/context
.filter-chip--accent  — Casa-blue active chip
.btn-sm-accent        — Casa-blue per-card primary CTA
.cat-{Guest|Cleaner|Pricing|Maintenance|Owner|Revenue|Pipeline|Compliance}  — category pill quartets
status-pill classes for pending/critical/medium/resolved
inner category-rule helper (2px bar, not a border-left)

<!-- Preserved patterns to reuse -->
.ex-card (globals.css:459) — exception-card base class to extend
.toast-row / .toast-undo (globals.css:1107-1139) — action receipt + Undo
.sheet / .sheet-overlay / .sheet-header — for the Pricing mega-card side-sheet
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract and extend the exception card into casa/exception-card.tsx</name>
  <read_first>
    - src/app/(dashboard)/page.tsx (the `ExceptionCard` function at lines 61-158 — the analog being extracted; also the `PublishButton` two-step confirm usage at 100-116)
    - src/components/ui/publish-button.tsx (the preserved `requiresConfirm` confirm pattern)
    - .planning/phases/01-casa-360-redesign/01-PATTERNS.md (section "src/components/casa/exception-card.tsx" — base structure to copy + the 6 required extensions)
    - .planning/phases/01-casa-360-redesign/01-UI-SPEC.md (Component Inventory "Exception card" row; Copywriting Contract per-category CTA table; Typography "Body (reading)" 15px tier; "8 Exception-Category color quartets")
  </read_first>
  <action>
    Create src/components/casa/exception-card.tsx as a `"use client"` component exporting `ExceptionCard`. It takes one `exception: ExceptionItem` prop plus action callbacks (`onAction`, `onDismiss`). Extract the structure from the existing `ExceptionCard` function in `(dashboard)/page.tsx:61-158` and extend it per the Pattern Map's 6 required additions:

    - Base: an `<article className="ex-card ...">` (reuse the `.ex-card` base class — extend, don't re-skin).
    - Category pill: render an 8-category color pill using the `.cat-{Category}` class created in plan 01-01, with the uppercase category text label as its content (color-is-never-alone — Checker item 6 / accessibility contract).
    - Inner 2px colored rule: the category accent — render it as the absolutely-positioned 2px inner bar (the `.kpi-card .kpi-rule` technique / the inner-rule helper from globals.css), NEVER a `border-left` width >1px (Checker item 2).
    - Time-ago: replace the static `timeAgo` string with `formatDistanceToNow(new Date(exception.createdAt), { addSuffix: true })` from `date-fns`, right-aligned, `tabular-nums`.
    - Title + subtitle: Playfair 18px title (`exception.typeLabel`); a `#737373` 13px property + neighborhood subtitle.
    - Body: the 2-3 sentence `exception.summary` at the 15px reading tier (`text-[15px]`, line-height 1.55).
    - `Suggested:` block: a `#F7F7F6` (`softgray`) filled block — `Suggested:` label in Inter 13px weight 500, the `exception.suggested` text in italic. Every card renders it (Checker item 6).
    - Action row: a primary button using `.btn-sm-accent` (Casa-blue, the per-category primary verb from the Copywriting Contract — one accent button per card, One Voice budget), a secondary `.btn-sm-outline` button, and a tertiary plain text link (`Dismiss`/`Resolve`/`Reject`/`Ignore`) in ink-soft `#737373`. Where `exception.requiresConfirm` is true, route the primary through the preserved `PublishButton` two-step confirm.
    - Source-attribution footer: `exception.source` with the booking ID in `.mono`, muted `#737373` text.

    All per-category copy comes from the UI-SPEC Copywriting Contract — do not invent verbs.
  </action>
  <verify>
    <automated>test -f src/components/casa/exception-card.tsx && grep -q 'export function ExceptionCard' src/components/casa/exception-card.tsx && grep -q 'formatDistanceToNow' src/components/casa/exception-card.tsx && grep -q 'Suggested' src/components/casa/exception-card.tsx && ! grep -nE 'border-left[^:]*:\s*[2-9]' src/components/casa/exception-card.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - src/components/casa/exception-card.tsx exists and exports `ExceptionCard`
    - the card renders a `.cat-*` category pill with an uppercase text label, an inner 2px rule (no `border-left` >1px), a `date-fns` time-ago, a Playfair title, a property+neighborhood subtitle, a 15px body, a `Suggested:` italic block, an action row, and a source footer
    - the primary action uses `.btn-sm-accent`; `requiresConfirm` actions route through `PublishButton`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>A reusable ExceptionCard renders the full narrative card contract from the UI-SPEC; the category accent is an inner rule, not a thick border.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the home page as the Exception Board</name>
  <read_first>
    - src/app/(dashboard)/page.tsx (the file being rewritten — KEEP: `"use client"`, `route-fade` root, the `useMemo` urgency-sort at 207-213, `URGENCY_RANK` import, the toast timer machinery at 216-239, the toast markup at 372-402; REMOVE: KPI tiles 314-353, right rail 423-517, status banner 253-305, `LEAD_KPI`/`SECONDARY_KPIS` 31-41)
    - src/lib/mock-data/exceptions.ts (the rewritten EXCEPTIONS + URGENCY_RANK from plan 01-02)
    - src/app/(dashboard)/pricing/page.tsx (the existing 26-row Approve/Edit/Reject bulk table — its markup is the preserved content of the Pricing mega-card side-sheet)
    - .planning/phases/01-casa-360-redesign/01-PATTERNS.md (section "src/app/(dashboard)/page.tsx — Exception Board")
    - .planning/phases/01-casa-360-redesign/01-UI-SPEC.md (Copywriting Contract — greeting eyebrow/headline/subtitle, empty-state copy; "4 Exception Board status pills"; Component Inventory "Greeting block" / "Status pill row" / "Filter chip row" / "Empty state" / "Pricing mega-card")
  </read_first>
  <action>
    Rewrite src/app/(dashboard)/page.tsx as the Exception Board. Keep `"use client"`, the `route-fade` root div, the toast timer machinery (`useRef<Map>` timers + `useEffect` cleanup) and the `.toast-row`/`.toast-undo` toast markup. Remove the 4 KPI tiles, the `status-banner`, the right rail `<aside className="home-rail">`, and the `LEAD_KPI`/`SECONDARY_KPIS` constants.

    Layout: a single column, `max-width:1000px`, centered, against the `#FAFAF7` route canvas (the canvas comes from the body background set in plan 01-01).

    Build top to bottom:
    - Greeting block: a role eyebrow from `useRole()` — `OWNER VIEW` / `OPERATIONS VIEW` (`.section-eyebrow`); a Playfair 40px headline `Good {timeOfDay}, {firstName}.` where `timeOfDay` is `morning` before 12:00 / `afternoon` 12:00-17:59 / `evening` 18:00+ computed from `new Date()`, and `firstName` is the first token of `user.name`; the subtitle `Here's what's waiting on you right now.`. This is the one Playfair Headline for this route (Checker item 5).
    - 4 status pills: `{n} pending`, `{n} critical`, `{n} medium`, `{n} resolved today` — counts computed from `EXCEPTIONS` (pending = open exceptions, critical/medium = by urgency, resolved-today = the locally-dismissed count). Use the status-pill classes from plan 01-01 (pending uses the Critical SOFT quartet). Animate count changes via opacity/transform only.
    - 7 multi-select filter chips: `All` (active by default), `Critical`, `Guest`, `Operations`, `Pricing`, `Pipeline`, `Compliance`. Model the multi-select on a `useState<Set<string>>` toggled per chip; the active chip uses `.filter-chip--accent`. `All` is mutually exclusive with the others.
    - Card stack: extend the existing `useMemo` urgency-sort comparator with the `created_at DESC` tiebreaker — `URGENCY_RANK[a.urgency] - URGENCY_RANK[b.urgency] || new Date(b.createdAt) - new Date(a.createdAt)`. Apply the active filter set, then render `casa/exception-card.tsx` per exception. Wire each card's actions through the preserved toast-with-Undo (including `Dismiss`, which optimistically removes the card from local state).
    - Empty state: when the filtered stack is empty, render the Playfair 40px `All clear.` + `Nothing in this category needs your attention right now.`
    - Pricing mega-card: the `pricing_week` exception renders as a "Pricing Week of [date]" card whose primary action opens a side-sheet (`.sheet` + `.sheet-overlay`) containing the 26-row Approve/Edit/Reject bulk table copied from `(dashboard)/pricing/page.tsx`. The bulk table is preserved workflow — relocate its markup into the sheet; the Approve/Edit/Reject buttons stay local-state/toast demos (real persistence is Phase 3).
  </action>
  <verify>
    <automated>grep -q 'useRole' src/app/(dashboard)/page.tsx && grep -q 'ExceptionCard' src/app/(dashboard)/page.tsx && grep -q 'All clear' src/app/(dashboard)/page.tsx && ! grep -qE 'home-rail|status-banner|SECONDARY_KPIS' src/app/(dashboard)/page.tsx && grep -cE 'font-display text-\[40px\]' src/app/(dashboard)/page.tsx | awk '{ if ($1 <= 2) exit 0; else exit 1 }' && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `/` renders a role-aware greeting (eyebrow + `Good {timeOfDay}, {firstName}.` headline + subtitle), 4 status pills, 7 filter chips, and an urgency-sorted ExceptionCard stack
    - the KPI tiles, status banner, right rail, and `SECONDARY_KPIS`/`LEAD_KPI` constants are gone
    - selecting a filter chip narrows the stack; an empty stack shows the `All clear.` empty state
    - a "Pricing Week of" mega-card opens a side-sheet with the 26-row bulk-approve table
    - exactly one Playfair 40px headline on the route (the greeting; the empty state replaces it when shown)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The home route is the Exception Board — a real operator can open `/`, read their day's exceptions, filter them, and reach the Pricing bulk-approve workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mock-data string -> rendered exception card | Exception `summary`/`suggested`/`source` strings render into card DOM |
| client role -> which exceptions surface | `useRole()` influences greeting copy and (optionally) card relevance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering / XSS | exception-card rendering of `summary`/`suggested`/`source` | mitigate | Render all mock-data strings as plain React text children only. The executor MUST NOT use `dangerouslySetInnerHTML` anywhere in `exception-card.tsx` or the Exception Board. React auto-escapes — no XSS sink introduced. |
| T-01-07 | Elevation of Privilege | role-driven exception filtering | accept | Role only tints the greeting and card relevance — all categories show for both roles per CONTEXT.md. No data is gated; role is not an authorization boundary in Phase 1 (mock data, no backend). |
| T-01-08 | Repudiation | local-state action buttons (Approve/Dismiss/Reject) | accept | Phase 1 actions are mock-only local-state/toast demos (L1) — no persistence, no audit trail by design. Real three-state lifecycle + `action_log` is Phase 3. Not a Phase 1 threat. |
</threat_model>

<verification>
- `npm run build` exits 0; no console errors when `/` loads.
- `/` shows no left sidebar, no KPI tiles, no right rail.
- Carlos sees `OWNER VIEW`; Denika sees `OPERATIONS VIEW`; the headline greeting matches the time of day.
- Every rendered card has a category pill, an inner 2px rule, a `Suggested:` italic block, and a source footer.
- Clicking a filter chip narrows the stack; deselecting all / a no-match filter shows `All clear.`
- The "Pricing Week of" card opens a side-sheet with the 26-row bulk-approve table.
</verification>

<success_criteria>
- ROADMAP SC2 satisfied: `/` is the Exception Board — greeting block, 4 status pills, 7 multi-select filter chips, urgency-sorted narrative exception cards (each with category pill, inner colored rule, property/neighborhood subtitle, 2-3 sentence body, `Suggested:` italic block, category-specific actions, source footer); KPI tiles + right rail removed; empty filter state shows `All clear.`
- ROADMAP SC4 (partial): the Pricing bulk-approve workflow is reachable as a "Pricing Week of [date]" mega-card opening the 26-row Approve/Edit/Reject side-sheet.
</success_criteria>

<output>
Create `.planning/phases/01-casa-360-redesign/01-03-SUMMARY.md` when done.
</output>
